**Changed:**

* Document that environment distance ``tol`` is a relative fraction
  (default 0.05 = +5% of the shortest distance), not an absolute
  length in angstroms. Clarified in
  ``SAF.features.environment.util`` docstrings and the binary feature
  table in ``doc/source/features.rst``.
